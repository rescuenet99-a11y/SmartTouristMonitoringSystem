# Flask Configuration
FLASK_APP=app.py
FLASK_ENV=development
FLASK_DEBUG=True
PORT=5000

# Database Configuration
# For MySQL database integration, configure the credentials below.
# By default, config.py will automatically fall back to a local SQLite database (sqlite:///trips.db)
# if MySQL connection details are not provided or if MySQL is not available.
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=sqlaarthi
DB_NAME=tourist_monitoring_db
DB_PORT=3306
